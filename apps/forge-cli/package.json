{
  "name": "@forge/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Forge CLI — command-line interface for the Forge AI IDE",
  "bin": {
    "forge": "./dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dev": "ts-node src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "rimraf": "^5.0.7"
  }
}
